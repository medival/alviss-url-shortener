apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alviss-url-shortener.fullname" . }}-test-connection"
  labels:
{{ include "alviss-url-shortener.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "alviss-url-shortener.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
